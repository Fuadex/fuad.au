# fuad.au hub — root ignores. Each app owns a nested .gitignore
# (rotation/.gitignore, culture/.gitignore).

# deploy staging, assembled by stage-site.js on every build
_site/

# OS / editor cruft
.DS_Store
Thumbs.db
